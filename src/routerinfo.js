import React from "react";

const ReactRouterInfo = () => {
  return (
    <div style={{ padding: "20px", fontFamily: "Arial, sans-serif" }}>
      <header style={{ textAlign: "center", marginBottom: "40px" }}>
        <h1 style={{ color: "#2c3e50" }}>React Router Tutorial</h1>
        <p style={{ fontSize: "18px", color: "#7f8c8d" }}>
          Learn how to manage navigation in React applications using React Router.
        </p>
      </header>

      <section>
        <h2 style={{ color: "#2980b9" }}>What is React Router?</h2>
        <p>
          React Router is a library for routing in React applications. It allows you to define routes for your application and navigate between different views or pages without refreshing the browser.
        </p>
      </section>

      <section>
        <h2 style={{ color: "#e67e22" }}>Features of React Router</h2>
        <ul style={{ lineHeight: "1.8" }}>
          <li><strong>Declarative Routing:</strong> Routes are declared as components.</li>
          <li><strong>Nested Routes:</strong> Support for hierarchical routing structures.</li>
          <li><strong>Dynamic Routing:</strong> Routes can be dynamically updated based on user actions or data changes.</li>
          <li><strong>Route Parameters:</strong> Pass parameters in URLs for dynamic content rendering.</li>
          <li><strong>Programmatic Navigation:</strong> Navigate programmatically using React Router hooks like `useNavigate`.</li>
        </ul>
      </section>

      <section>
        <h2 style={{ color: "#16a085" }}>Basic Example</h2>
        <p>Here's how to set up React Router in your application:</p>
        </section>
        
        </div>)}
        export default ReactRouterInfo

